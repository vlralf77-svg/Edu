# 가족통화 앱 환경변수
# 값을 채운 뒤 이 파일을 `.env` 로 복사하세요.

# (선택) 자체 호스팅한 PeerServer 브로커 주소.
# 비워두면 PeerJS 공개 브로커(0.peerjs.com)를 사용합니다.
# 예: VITE_PEER_HOST=peer.mydomain.com
VITE_PEER_HOST=
VITE_PEER_PORT=443
VITE_PEER_PATH=/
VITE_PEER_SECURE=true
